@AGENTS.md

# Branch Policy
**Always use `main` as the default branch. Never push to or branch from `master`.**
The remote `origin/master` exists but is legacy — `origin/main` is the canonical branch and what Vercel deploys from.
